install the following libraries and make sure that the system is having an active network connection


pip install gTTs
pip install tkinder
pip install playsound
pip install googletrans
pip install googletrans==3.1.0a0
